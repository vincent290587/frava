<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/route_popup"
    android:layout_width="300dp"
    android:layout_height="200dp"
    android:background="#FFFFFF"
    android:longClickable="false">

    <TextView
        android:id="@+id/route_name_send"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="30dp"
        android:layout_marginTop="30dp"
        android:layout_marginEnd="30dp"
        android:text="Sending route..."
        android:textSize="22sp" />

    <ProgressBar
        android:id="@+id/progress_bar_route"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="270dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/route_name_send"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp" />

    <Button
        android:id="@+id/button_cancel"
        style="@style/Widget.AppCompat.Button"
        android:layout_width="106dp"
        android:layout_height="55dp"
        android:layout_below="@+id/progress_bar_route"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="10dp"
        android:layout_marginTop="34dp"
        android:layout_marginBottom="5dp"
        android:background="#C1E0FB"
        android:clickable="true"
        android:ellipsize="none"
        android:text="Cancel" />

    <Button
        android:id="@+id/button_send"
        android:layout_width="106dp"
        android:layout_height="55dp"
        android:layout_below="@+id/progress_bar_route"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="34dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="5dp"
        android:background="#C1E0FB"
        android:text="Send" />

</RelativeLayout>